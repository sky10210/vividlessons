name: Wire Basics of Marketing Sheet

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/wire-basics-marketing-sheet.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch marketing module submission receiver
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          p = Path('modules/marketing-basics-intro-mrd.html')
          s = p.read_text()
          endpoint = 'https://script.google.com/macros/s/AKfycbw4kLfppzaPH0LJnYnijygKpqSN11gme9JHpgOt4_R-ReiA-cHFi53kde4hK3kl0y4/exec'
          s = re.sub(r'const COMPLETION_WEB_APP_URL = "[^"]+";', f'const COMPLETION_WEB_APP_URL = "{endpoint}";', s, count=1)

          start = s.find('function submitCompletionRecord(record){')
          end = s.find('\n\nrestoreCompletionLockIfNeeded();', start)
          if start == -1 or end == -1:
              raise SystemExit('Could not locate marketing submission function')

          new_func = '''function submitCompletionRecord(record){
  if(completionSubmitted){
    toast("Completion already submitted.");
    return;
  }

  completionSubmitted = true;
  toast("Saving completion...");

  const p = new URLSearchParams({
    bellId: "MARKETING-BASICS",
    label: "Basics of Marketing",
    firstName: record.firstName || "",
    lastName: record.lastName || "",
    name: ((record.firstName||"")+" "+(record.lastName||"")).trim(),
    answer: JSON.stringify({
      score: record.score,
      totalQuestions: record.totalQuestions,
      percent: record.percent,
      reflection: record.reflection,
      version: record.version,
      questionTypes: record.questionTypes,
      answers: record.answers,
      sessionId: record.sessionId,
      timeStarted: record.timeStarted,
      timeFinished: record.timeFinished
    }),
    late: "0",
    duration: String(record.totalTime || "").replace(/[^0-9]/g, "") || "1",
    timestamp: record.timestamp || new Date().toISOString(),
    cls: "Marketing - " + (record.classPeriod || "")
  });

  fetch(`${COMPLETION_WEB_APP_URL}?${p.toString()}`,{
    method:"GET",
    mode:"no-cors"
  })
  .then(()=>{
    toast("Completion saved to Google Sheets.");
  })
  .catch(error=>{
    completionSubmitted = false;
    console.error("Completion submit error:", error);
    toast("Certificate created, but completion did not save. Tell your teacher.");
  });
}'''

          s = s[:start] + new_func + s[end:]
          p.write_text(s)
          PY
      - name: Commit connection
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add modules/marketing-basics-intro-mrd.html
          if git diff --cached --quiet; then
            echo 'Already wired.'
          else
            git commit -m 'Wire Basics of Marketing to Opening Bell workbook'
            git push
          fi
# trigger 2026-08-24
